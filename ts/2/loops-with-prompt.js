/**
 * Run a loop forever and ask everytime the user to enter a character
 * If the charater is 'n' stop running the program, 
 * otherwise continue running the loop
 * 
 */
var prompt = require('prompt');
prompt.start();
prompt.get(['n'], function(err, rslt)  {
var n = "n"
    while(n = false) { 
prompt.get(['n'], function(err, result)  {


//while("n" = false) {
//prompt.get(['n'], function(err, result)  {
//});
//};
});
};
});


//Assign a value Ayush to a variable and print to the console the value of that variable
//var p = "Ayush"
//console.log(p)

//Assign a value A to a variable and print to the console the value of that variable
//var m = "a"
//console.log(m)